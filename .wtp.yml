# Worktree Plus Configuration
version: "1.0"

defaults:
  base_dir: .worktrees

hooks:
  post_create:
    - type: copy
      from: .env        # Copy actual .env file (gitignored)
      to: .env

    - type: command
      command: bun install
